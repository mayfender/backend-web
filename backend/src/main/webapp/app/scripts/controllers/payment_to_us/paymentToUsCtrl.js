angular.module('sbAdminApp').controller('PaymentToUsCtrl', function($rootScope, $scope, $base64, $http, $translate, $localStorage, $state, urlPrefix) {
	
	console.log('Payment to us ');
	$scope.bankIcon = urlPrefix + '/app/images/scb.png';

});