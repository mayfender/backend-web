


----------------------------------------------
----: วันนี้วันที่ 06/09/2016
--: ได้ลองทำการติดตั้งระบบจริง เป็นครั้งแรก
--: เริ่มจาก install MongoDB database ซึ่งใช้เวลาพอสมควร เนื่งจากติดปัญหาเรื่อง MongoDB client (VUE) ไม่สามารถดูข้อมมูลผ่านการ connect แบบ remote
--: จึงได้ลองหา tool ตัวอื่นมาใช้ดู (robomongo) ปรากฏว่าใช้งานได้  
--: เมื่อ database ทำงานได้ ต่อมาก็ install Java runtime (JRE) และ Apache tomcat 2 ตัวนี้ไม่มีปัญหาอะไร
----: เริ่มทำงานจริง
--: ตั้งค่าการใช้งานระบบ เพิ่ม product,actionCode,resultCode,แบบฟอร์มออกรายงานการติดตาม,แบบฟอร์ม-notice,load งานใหม่

---: ปัญหาที่พบ

---: แบบฟอร์ม-notice 
--: ที่อยู่ใน account list ของ summit ไม่ได้แบ่งเป็นส่วน คือให้มาเป็น column เดียว
--: program เดิมทำเป็น pdf แต่ตัว DMS version 1 ให้เป็น word ไปก่อน

---: load งานใหม่
--: summit ไฟล์ account list มี header ที่ซ้ำกัน  ต้องแก้ใข program ให้ rename โดยต่อท้าย header ด้วย  _1,_2,_3,...
--: ต้องมีระบบ check หลังจาก load งานว่า ข้อมูลทั้งหมดถูก load เข้าถูกต้อง  อาจจะมีปุ่ม export ให้ download excel โดยให้ excel ดึงข้อมูลจาก database
--: แก้ปัญหาเรื่อง id_card ไม่มีใน account list  เพราะในการแสดงบัญชีพ่วงมันจะไปดึง record ที่ id card ว่างมาทั้งหมด ซึ่งผิด
--: ปกติวิธีการ assign งานจะทำผ่าน excel โดยผู้มีหน้าที่ assign อาจเอางานกลับไปทำที่บ้าน  ด้วยเหตุนี้ ระบบจะต้อง provide การ assign งานแบบ load excel เข้าไปด้วย
           ซึ่งใน excel จะมีข้อมูล contract_no ผูกกับ collect 
           
---: New requirement
--: น้องอยากให้เปิดโปรแกรมได้หลาย instance แต่ปกติ DMS ใช้ token save ไว้ที่เครื่อง client ทำให้ยากต่อการ เปิดขึ้นมาหลายตัว #FIXED
--: วิธีที่ดีกว่านั้นคือ  ทำให้ DMS สามารถ search ข้อมูลได้ต่างกันหลาย tab (ใช้ tab เข้ามาช่วย) #FIXED
--: หน้างาน การเลือกเคสมาทำงานต้องสลับ tab ยังไม่ค่อยสะดวก อยากให้มีปุ่ม next previous เพื่อนดึงงาน case ต่อไปหรือ ก่อนหน้ามาทำได้เลย
--: หน้างานตรง tab account list กดบันทึกแล้วมัน reorder ใหม่ทำให้งานดูแล้วเหมือนหายไป ไม่อยากให้ reorder ใหม่


